// Minimal Arduino.h shim so the OSC library can be compiled with a host compiler.
#ifndef HOST_ARDUINO_H
#define HOST_ARDUINO_H

#include <stdint.h>
#include <stdlib.h>
#include <string.h>
#include <limits.h>

typedef bool boolean;
typedef uint8_t byte;

#endif
